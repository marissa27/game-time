var assert = require('chai').assert
// var testing = require('../lib/index');

describe('testing chai', function () {

  it('testing', function () {
    assert.equal(testing(), 'test works');
  });
});

describe('Timer', function() {
  it('reset on level start')

  it('load time limit from current level on start')

  it('trigger game over at t=0 seconds')

})

describe('Game Object', function () {
  it('should save a lives')

  it('should save the level')

  it('should start the timer?')

})

describe('Reset the Level', function() {
  it('reset the map (reload the grid)')
  it('reduce lives by one')
})
